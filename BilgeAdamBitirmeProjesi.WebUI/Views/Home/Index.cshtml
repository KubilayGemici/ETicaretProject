@model List<ProductViewModel>
@{
    ViewData["Title"] = "Default";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 padding-right">
                <div class="features_items">
                    <h2 class="title text-center">Rüya Ev Ürünler</h2>
                    @foreach (ProductViewModel item in Model)
                    {

                        <div class="col-sm-4">
                            <div class="product-image-wrapper">
                                <div class="single-products">
                                    <div class="productinfo text-center">
                                        <img src="@item.Image" alt="" />
                                        <h2>@item.Price</h2>
                                        <p>@item.ProductName</p>
                                        <ul>
                                            <i class="fa fa-shopping-cart"></i>@Html.ActionLink("Sepete Ekle", "AddCartCustom", new { id = ViewBag.basket })
                                        </ul>
                                    </div>
                                </div>
                                <div class="choose">
                                    <ul class="nav nav-pills nav-justified">
                                        <li><a asp-controller="Home" asp-action="Product" asp-route-id="@item.Id" title=""><i class="fa fa-plus-square"></i>Ürün Hakkında</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>
